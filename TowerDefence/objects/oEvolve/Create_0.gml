/// @description Store important variables

//This object handles tower evolution and UI

tower = noone; //A string containing the object that which is being evolved
towerInstance = noone; //The id of the actual instance being evolved
rangerUI = noone; //The id of the Ranger evolveUI instance, so it can be deleted
mageUI = noone; //The id of the Mage evolveUI instance, so it can be deleted